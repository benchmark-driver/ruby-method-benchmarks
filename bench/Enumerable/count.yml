-
  description: Enumerable#count
  before: |-
    enum = [1, 2, 4, 2].to_enum
  benchmark: |-
    enum.count
-
  description: Enumerable#count(item)
  before: |-
    enum = [1, 2, 4, 2].to_enum
  benchmark: |-
    enum.count(2)
-
  description: Enumerable#count { |obj| true }
  before: |-
    enum = [1, 2, 4, 2].to_enum
  benchmark: |-
    enum.count { |obj| true }
-
  description: Enumerable#count { |obj| false }
  before: |-
    enum = [1, 2, 4, 2].to_enum
  benchmark: |-
    enum.count { |obj| false }
