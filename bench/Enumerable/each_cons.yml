-
  description: Enumerable#each_cons(n)
  before: |-
    enum = (1..100).to_enum
  benchmark: |-
    enum.each_cons(5)
-
  description: Enumerable#each_cons(n) { |obj| ...}
  before: |-
    enum = (1..100).to_enum
  benchmark: |-
    enum.each_cons(5) { |obj| obj }
