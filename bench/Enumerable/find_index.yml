-
  description: Enumerable#find_index
  before: |-
    enum = [1, 2, 3, 4, 5].to_enum
  benchmark: |-
    enum.find_index
-
  description: Enumerable#find_index(value)
  before: |-
    enum = [1, 2, 3, 4, 5].to_enum
  benchmark: |-
    enum.find_index(5)
-
  description: Enumerable#find_index { |obj| true }
  before: |-
    enum = [1, 2, 3, 4, 5].to_enum
  benchmark: |-
    enum.find_index { |obj| true }
-
  description: Enumerable#find_index { |obj| false }
  before: |-
    enum = [1, 2, 3, 4, 5].to_enum
  benchmark: |-
    enum.find_index { |obj| false }
