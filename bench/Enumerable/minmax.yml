-
  description: Enumerable#minmax
  before: |-
    enum = (1..100).to_enum
  benchmark: |-
    enum.minmax
-
  description: Enumerable#minmax { |a, b| ...}
  before: |-
    enum = (1..100).to_enum
  benchmark: |-
    enum.minmax { |a, b| a <=> b }
