-
  description: Enumerable#one? { |obj| true }
  before: |-
    enum = (1..100).to_enum
  benchmark: |-
    enum.one? { |obj| true }
-
  description: Enumerable#one? { |obj| false }
  before: |-
    enum = (1..100).to_enum
  benchmark: |-
    enum.one? { |obj| false }
