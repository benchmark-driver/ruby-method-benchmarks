---
benchmark:
- name: Enumerable#all? { |obj| true }
  script: enum.all? { |obj| true }
  prelude: enum = (1..100).to_enum
- name: Enumerable#all? { |obj| false }
  script: enum.all? { |obj| false }
  prelude: enum = (1..100).to_enum
