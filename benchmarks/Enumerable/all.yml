-
  description: Enumerable#all? { |obj| true }
  before: |-
    enum = (1..100).to_enum
  benchmark: |-
    enum.all? { |obj| true }
-
  description: Enumerable#all? { |obj| false }
  before: |-
    enum = (1..100).to_enum
  benchmark: |-
    enum.all? { |obj| false }
