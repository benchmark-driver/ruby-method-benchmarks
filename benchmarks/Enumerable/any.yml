-
  description: Enumerable#any? { |obj| true }
  before: |-
    enum = (1..100).to_enum
  benchmark: |-
    enum.any? { |obj| true }
-
  description: Enumerable#any? { |obj| false }
  before: |-
    enum = (1..100).to_enum
  benchmark: |-
    enum.any? { |obj| false }
