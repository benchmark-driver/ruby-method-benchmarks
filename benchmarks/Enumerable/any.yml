---
benchmark:
- name: Enumerable#any? { |obj| true }
  script: enum.any? { |obj| true }
  prelude: enum = (1..100).to_enum
- name: Enumerable#any? { |obj| false }
  script: enum.any? { |obj| false }
  prelude: enum = (1..100).to_enum
