---
benchmark:
- name: Enumerable#cycle(n)
  script: enum.cycle(2)
  prelude: enum = (1..100).to_enum
- name: Enumerable#cycle(n) { |obj| ...}
  script: enum.cycle(2) { |obj| }
  prelude: enum = (1..100).to_enum
