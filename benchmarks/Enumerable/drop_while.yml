-
  description: Enumerable#drop_while
  before: |-
    enum = (1..100).to_enum
  benchmark: |-
    enum.drop_while
-
  description: Enumerable#drop_while { |obj| true }
  before: |-
    enum = (1..100).to_enum
  benchmark: |-
    enum.drop_while { |obj| true }
-
  description: Enumerable#drop_while { |obj| false }
  before: |-
    enum = (1..100).to_enum
  benchmark: |-
    enum.drop_while { |obj| false }
