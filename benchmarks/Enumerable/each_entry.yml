---
benchmark:
- name: Enumerable#each_entry
  script: enum.each_entry
  prelude: enum = (1..100).to_enum
- name: Enumerable#each_entry { |obj| ...}
  script: enum.each_entry { |obj| }
  prelude: enum = (1..100).to_enum
