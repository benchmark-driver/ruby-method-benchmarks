---
benchmark:
- name: Enumerable#each_with_index
  script: enum.each_with_index
  prelude: enum = (1..100).to_enum
- name: Enumerable#each_with_index { |obj, index| ...}
  script: enum.each_with_index { |obj, index| }
  prelude: enum = (1..100).to_enum
