---
benchmark:
- name: Enumerable#group_by
  script: enum.group_by
  prelude: enum = (1..100).to_enum
- name: Enumerable#group_by
  script: enum.group_by { |x| x % 3 }
  prelude: enum = (1..100).to_enum
