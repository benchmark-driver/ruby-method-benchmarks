-
  description: Enumerable#include?(obj)
  before: |-
    enum = (1..100).to_enum
  benchmark: |-
    enum.include?(200)
-
  description: Enumerable#member?(obj)
  before: |-
    enum = (1..100).to_enum
  benchmark: |-
    enum.member?(200)
