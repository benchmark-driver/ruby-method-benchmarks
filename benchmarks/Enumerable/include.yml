---
benchmark:
- name: Enumerable#include?(obj)
  script: enum.include?(200)
  prelude: enum = (1..100).to_enum
- name: Enumerable#member?(obj)
  script: enum.member?(200)
  prelude: enum = (1..100).to_enum
