---
benchmark:
- name: Enumerable#none? { |obj| true }
  script: enum.none? { |obj| true }
  prelude: enum = (1..100).to_enum
- name: Enumerable#none? { |obj| false }
  script: enum.none? { |obj| false }
  prelude: enum = (1..100).to_enum
