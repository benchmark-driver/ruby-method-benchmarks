-
  description: Enumerable#none? { |obj| true }
  before: |-
    enum = (1..100).to_enum
  benchmark: |-
    enum.none? { |obj| true }
-
  description: Enumerable#none? { |obj| false }
  before: |-
    enum = (1..100).to_enum
  benchmark: |-
    enum.none? { |obj| false }
