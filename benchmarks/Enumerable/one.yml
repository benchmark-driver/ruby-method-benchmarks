---
benchmark:
- name: Enumerable#one? { |obj| true }
  script: enum.one? { |obj| true }
  prelude: enum = (1..100).to_enum
- name: Enumerable#one? { |obj| false }
  script: enum.one? { |obj| false }
  prelude: enum = (1..100).to_enum
