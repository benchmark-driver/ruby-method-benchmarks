---
benchmark:
- name: Enumerable#reverse_each
  script: enum.reverse_each
  prelude: enum = (1..100).to_enum
- name: Enumerable#reverse_each { |obj| ...}
  script: enum.reverse_each { |obj| }
  prelude: enum = (1..100).to_enum
