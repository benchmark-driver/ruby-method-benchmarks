-
  description: Enumerable#sort_by
  before: |-
    enum = [1, 2, 3, 4, 2.5, 3.2, 4.6].to_enum
  benchmark: |-
    enum.sort_by
-
  description: Enumerable#sort_by { |a, b| ...}
  before: |-
    enum = [1, 2, 3, 4, 2.5, 3.2, 4.6].to_enum
  benchmark: |-
    enum.sort_by { |a, b| a <=> b }
