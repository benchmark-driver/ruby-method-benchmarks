---
benchmark:
- name: Hash#[key]=(value)
  script: hash["baz"] = 42
  prelude: hash = { "foo" => 100, "bar" => 300 }
- name: Hash#store(key, value)
  script: hash.store("baz", 42)
  prelude: hash = { "foo" => 100, "bar" => 300 }
