---
benchmark:
- name: Hash#compact
  script: |-
    h = hash.dup
    h.compact
  prelude: 'hash = { a: 1, b: false, c: nil }'
- name: Hash#compact!
  script: |-
    h = hash.dup
    h.compact!
  prelude: 'hash = { a: 1, b: false, c: nil }'
