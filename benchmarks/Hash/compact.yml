---
benchmark:
- name: Hash#compact
  script: |-
    h = hash.dup
    h.compact
  prelude: 'hash = { a: 1, b: false, c: nil }'
  required_ruby_version: 2.4
- name: Hash#compact!
  script: |-
    h = hash.dup
    h.compact!
  prelude: 'hash = { a: 1, b: false, c: nil }'
  required_ruby_version: 2.4
