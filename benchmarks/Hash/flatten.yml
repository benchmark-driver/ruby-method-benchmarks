---
benchmark:
- name: flatten
  script: hash.flatten
  prelude: hash = {1=> "one", 2 => [2,"two"], 3 => "three"}
- name: flatten(0)
  script: hash.flatten(0)
  prelude: hash = {1=> "one", 2 => [2,"two"], 3 => "three"}
- name: flatten(2)
  script: hash.flatten(2)
  prelude: hash = {1=> "one", 2 => [2,"two"], 3 => "three"}
