---
benchmark:
- name: Range#include?(obj)
  script: range.include?(10)
  prelude: range = 0..100
- name: Range#include?(obj) (not included)
  script: range.include?(200)
  prelude: range = 0..100
- name: Range#member?(obj)
  script: range.member?(10)
  prelude: range = 0..100
- name: Range#member?(obj) (not included)
  script: range.member?(200)
  prelude: range = 0..100
