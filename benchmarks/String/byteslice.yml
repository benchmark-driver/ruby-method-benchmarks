---
benchmark:
- name: String#byteslice(nth)
  script: string.byteslice(1)
  prelude: string = "hello"
- name: String#byteslice(nth, len)
  script: string.byteslice(1, 2)
  prelude: string = "hello"
- name: String#byteslice(range)
  script: string.byteslice(1..2)
  prelude: string = "hello"
