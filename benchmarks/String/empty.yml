---
benchmark:
- name: String#empty?
  script: str.empty?
  prelude: str = ""
- name: String#empty? with ASCII
  script: str.empty?
  prelude: str = "Hello there"
- name: String#empty? with UTF8
  script: str.empty?
  prelude: str = "あいうえお"
